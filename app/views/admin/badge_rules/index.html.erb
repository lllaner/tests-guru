<table class="table table-hover my-3">
      <thead class="thead-dark">
        <tr class="text-center">
          <th>Badge rules</th>
          <th colspan="3"> Action</th>
        </tr>
      </thead>
      <tbody>
        <%= render @badge_rules %>
      </tbody>
    </table>
<%= link_to t('.create'), new_admin_badge_rule_path, class: 'btn btn-success'%>
