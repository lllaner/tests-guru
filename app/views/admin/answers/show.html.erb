<h1> Question - <%= @answer.question.body %></h1>
<p class="h2 my-3">Answer: <%= @answer.body %></p>
<%= link_to 'Edit', edit_admin_answer_path(@answer), class: 'btn btn-info' %>
<%= link_to 'Back', admin_question_path(@answer.question), class: 'btn btn-primary' %>
