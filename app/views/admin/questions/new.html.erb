<div class="row justify-content-center">
  <div class="col col-md-6">
    <h1><%= question_header(@question) %></h1>
    <%= render 'form' %>
    <%= link_to 'Back', admin_test_path(@test), class: 'btn btn-primary' %>
  </div>
</div>
