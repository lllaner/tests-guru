<tr class="text-center">
  <td><%= question.body %></td>
  <td><%= link_to 'Show', admin_question_path(question), class: 'text-info' %></td>
  <td><%= link_to 'Edit', edit_admin_question_path(question), class: 'text-important' %></td>
  <td><%= link_to 'Delete',
              admin_question_path(question),
              class: 'text-danger',
              method: :delete,
              data:   { confirm: 'Are you sure?' }%></td>
</tr>
